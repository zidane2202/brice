# Landing + next-intl Implementation Plan

> **For agentic workers:** Execute task-by-task on `main`.

**Goal:** Public marketing landing on `/` with FR/EN (next-intl), pricing from plans.ts, WhatsApp contact; logged-in users redirect to dashboard.

**Architecture:** Make `/` public in proxy; next-intl with fr default + en; landing components under `src/components/landing/`; light marketing visual distinct from dark app shell.

**Tech Stack:** Next.js 16, next-intl, existing CSS vars + landing fonts for landing

## Global Constraints

- Work on main
- Contact via NEXT_PUBLIC_SUPPORT_WHATSAPP
- Pricing from src/lib/plans.ts
- No chatbot on landing V1
- Frontend design: brand-first hero, full-bleed atmosphere, no purple/cream-terracotta AI defaults, expressive fonts

---

### Task 1: Install next-intl + message files + config

### Task 2: proxy public `/` + page redirect if session

### Task 3: Landing UI sections FR/EN + locale switcher

### Task 4: Build + commit
