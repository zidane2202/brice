"use server";
import { revalidatePath } from "next/cache";
import { createSupabaseAdmin } from "@/lib/supabase-admin";
import { getUser, getUserProfile } from "@/lib/supabase-server";

export async function createSupportTicket(formData:FormData){const user=await getUser();if(!user)throw new Error("Non authentifié");const subject=String(formData.get("subject")??"").trim().slice(0,120);const body=String(formData.get("body")??"").trim().slice(0,3000);const priority=formData.get("priority")==="urgent"?"urgent":"normal";if(subject.length<3||!body)throw new Error("Sujet et message obligatoires");const db=createSupabaseAdmin();const{data:ticket,error}=await db.from("support_tickets").insert({user_id:user.id,subject,priority}).select("id").single();if(error)throw new Error(error.message);const{error:messageError}=await db.from("support_messages").insert({ticket_id:ticket.id,user_id:user.id,author_role:"reseller",body});if(messageError)throw new Error(messageError.message);revalidatePath("/support");}
export async function replySupportTicket(formData:FormData){const user=await getUser();const profile=await getUserProfile();if(!user||profile?.role!=="admin")throw new Error("Accès refusé");const ticketId=String(formData.get("ticket_id")??"");const body=String(formData.get("body")??"").trim().slice(0,3000);const status=String(formData.get("status")??"in_progress");if(!ticketId||!body||!["open","in_progress","resolved"].includes(status))throw new Error("Réponse invalide");const db=createSupabaseAdmin();const{data:ticket}=await db.from("support_tickets").select("user_id").eq("id",ticketId).single();if(!ticket)throw new Error("Ticket introuvable");await db.from("support_messages").insert({ticket_id:ticketId,user_id:user.id,author_role:"admin",body});await db.from("support_tickets").update({status,updated_at:new Date().toISOString()}).eq("id",ticketId);await db.from("user_notifications").upsert({user_id:ticket.user_id,type:"support_reply",title:"Le support vous a répondu",body:body.slice(0,160),url:"/support",dedup_key:`support-${ticketId}-${Date.now()}`},{onConflict:"user_id,dedup_key"});revalidatePath("/admin/support");}
